language: node_js
node_js: node

before_script:
- npm run build

script:
- npm run ci

after_success:
- npm run cov

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    branch: master