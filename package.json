{
  "name": "double.js",
  "version": "0.0.2",
  "description": "Floating point expansions with 32 accurate decimal digits, also known as double-double arithmetic.",
  "main": "src/double.js",
  "scripts": {
    "build": "browserify src/double.js > dist/double.iife.js",
    "test": "istanbul cover ./test/test.js --report lcovonly -- -R spec",
    "bnt": "npm run build && npm run test"
  },
  "keywords": [
    "double-double",
    "quad-double",
    "arbitrary-precision",
    "floating-point"
  ],
  "author": "munrocket",
  "license": "MIT ",
  "devDependencies": {
    "browserify": "^16.2.2",
    "codecov": "^3.0.4",
    "istanbul": "^0.4.5",
    "tap-spec": "^5.0.0",
    "tape": "^4.9.1"
  }
}
